name: 'imoveis_pipeline'
version: '1.0.0'
config-version: 2

profile: 'imoveis_pipeline'

model-paths: ['models']
test-paths: ['tests']
seed-paths: ['seeds']

models:
  imoveis_pipeline:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table

vars:
  minio_bucket: 'imoveis-processed'
